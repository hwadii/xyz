// @ts-expect-error
import { indexBy } from "../source/index-by";
import test from "ava";

test.todo("creates an object with each index as keys");

test.todo("creates an empty object when input is empty");
